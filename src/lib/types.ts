export interface User {
  id: string;
  firstName: string;
  fullName: string;
  lastName: string;
  email: string;
  mesa: number;
  register: number | null;
  createdAt: number | null;
  updatedAt: number | null;
}
